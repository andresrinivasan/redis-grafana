#!/bin/sh

ID=$(id -u)
HOME=$(getent passwd "${ID}" | cut -f6 -d:)

docker run --rm -d --user "${ID}" --volume "${HOME}"/lib/grafana:/var/lib/grafana --name grafana --network host grafana/grafana:latest

